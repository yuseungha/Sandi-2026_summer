# SafeNest CO2 작업 규칙

- 실기기 실행·펌웨어 업로드·`git push`·PR 생성은 운영자 승인 후에만 한다.
- 팀 저장소(`jinsu1011/...`)에 push 금지. 산출물은 `yuseungha/Sandi-2026_summer` 에만 둔다.
- 확인되지 않은 값은 "확인되지 않음"으로 표기. 추정치를 결과로 쓰지 않는다.
- 문서와 실제 저장소가 다르면 **실제 저장소가 사실**이고, 차이는 보고서에 남긴다.
- 원본 데이터는 어떤 경우에도 수정하지 않는다. 파생물은 별도 파일로 만든다.
- 자격증명(WiFi PW 등)을 커밋하지 않는다. 발견 시 즉시 보고.
- mmWave / Thermal 코드 수정 금지.
- 정식 검증 성능 수치(accuracy/F1) 산출·기재 금지. 현재 단계는 탐색 실측이다.
